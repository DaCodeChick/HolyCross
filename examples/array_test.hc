// Test array features
U0 Main() {
    // Array declaration
    I64 arr[10];
    
    // Array element access
    arr[0] = 42;
    arr[5] = 100;
    
    // Array element read
    I64 x;
    x = arr[0];
    x = arr[5];
    
    "Array test\n";
}
